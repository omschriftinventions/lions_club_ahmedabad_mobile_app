# Mobile app runtime config. Copy to .env (dev) or .env.production (EAS build).
# Expo exposes EXPO_PUBLIC_* vars to the app via process.env at bundle time.
EXPO_PUBLIC_API_URL=https://api.lionsclubofahmedabadhost.com
